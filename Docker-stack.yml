version: "3"
services:
  web:
  #your Image detials 
  image: adhiar/webserver:1
  deploy:
    replicas: 5
    resources:
      limits:
      cpus: "0.1"
      memory: 50M
    restart_policy:
      condition: on-failure
    ports:
      - "3001:80"
    network:
      - xVswitch